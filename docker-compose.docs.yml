services:
  docs:
    image: e_commerce_app_local_docs
    container_name: e_commerce_app_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./e_commerce_app:/app/e_commerce_app:z
    ports:
      - '9000:9000'
    command: /start-docs
